<?xml version="1.0" encoding="UTF-8"?>

<launch>
  <!-- Arguments for launch file with defaults provided -->
  <arg name="database_path"     default="rtabmap.db"/>
  <arg name="rgb_topic"   default="/camera/color/image_raw"/>
  <arg name="depth_topic" default="/camera/depth/image_raw"/>
  <arg name="camera_info_topic" default="/camera/color/camera_info"/> 

  <!-- Localization Node -->
  <group ns="rtabmap">
    <node name="rtabmap" pkg="rtabmap_slam" type="rtabmap" output="screen">

      <!-- Basic RTAB-Map Parameters -->
      <param name="database_path"       type="string" value="$(arg database_path)"/>
      <param name="frame_id"            type="string" value="base_footprint"/>
      <param name="odom_frame_id"       type="string" value="odom"/>
      <param name="subscribe_depth"     type="bool"   value="true"/>
      <param name="subscribe_scan"      type="bool"   value="true"/>
      <param name="subscribe_scan_cloud" type="bool"   value="true"/>
     
      <!-- RTAB-Map Inputs -->
      <remap from="scan"            to="/scan_multi"/>
      <remap from="scan_cloud"      to="/unitree_lidar_l1/points"/>
      <remap from="rgb/image"       to="$(arg rgb_topic)"/>
      <remap from="depth/image"     to="$(arg depth_topic)"/>
      <remap from="rgb/camera_info" to="$(arg camera_info_topic)"/>
      <remap from="odom"            to="/odometry/filtered"/>
      
      <!-- Sonar sensors -->
      <!-- <param name="subscribe_sonar" type="bool" value="true"/>
      <remap from="sonar1" to="/sonar_1"/>
      <remap from="sonar2" to="/sonar_2"/>
      <remap from="sonar3" to="/sonar_3"/>
      <remap from="sonar4" to="/sonar_4"/>
      <remap from="sonar5" to="/sonar_5"/>
      <remap from="sonar6" to="/sonar_6"/> -->

      <!-- RTAB-Map Output -->
      <remap from="grid_map" to="/map"/>

      <!-- Disable mapping (localization mode) -->
      <param name="Mem/IncrementalMemory" type="string" value="false"/>
      <param name="Mem/InitWMWithAllNodes" type="string" value="true"/>

      <!-- Loop Closure Detection -->
      <param name="Kp/DetectorStrategy" type="string" value="0"/> 
      <param name="Kp/MaxFeatures" type="string" value="1000"/>  
      <param name="SURF/HessianThreshold" type="string" value="100"/>

      <!-- Loop Closure Constraint -->
      <param name="Reg/Strategy" type="string" value="1"/> 
      <param name="Vis/MinInliers" type="string" value="20"/>
      <param name="Vis/InlierDistance" type="string" value="0.1"/>

      <!-- Optimization -->
      <param name="RGBD/NeighborLinkRefining" type="string" value="true"/>
      <param name="Optimizer/Strategy" type="string" value="1"/> 
      <param name="Optimizer/Iterations" type="string" value="100"/>
      <param name="Reg/VarianceFromInliersCount" type="string" value="true"/>

      <!-- Additional sensor fusion -->
      <param name="subscribe_imu" type="bool" value="true"/>
      <remap from="imu" to="/imu"/>
    </node> 

    <!-- visualization with rtabmapviz -->
    <node pkg="rtabmap_viz" type="rtabmap_viz" name="rtabmapviz" args="-d $(find rtabmap_ros)/launch/config/rgbd_gui.ini" output="screen">
      <param name="subscribe_depth" type="bool" value="true"/>
      <param name="subscribe_scan" type="bool" value="true"/>
      <param name="frame_id" type="string" value="base_footprint"/>

      <remap from="rgb/image"       to="$(arg rgb_topic)"/>
      <remap from="depth/image"     to="$(arg depth_topic)"/>
      <remap from="rgb/camera_info" to="$(arg camera_info_topic)"/>
      <remap from="scan"            to="/scan_multi"/>
      <remap from="scan_cloud"      to="/unitree_lidar_l1/points"/>
      <remap from="odom"            to="/odometry/filtered"/>
      
      <!-- Sonar visualization -->
      <!-- <param name="subscribe_sonar" type="bool" value="true"/>
      <remap from="sonar1" to="/sonar_1"/>
      <remap from="sonar2" to="/sonar_2"/>
      <remap from="sonar3" to="/sonar_3"/>
      <remap from="sonar4" to="/sonar_4"/>
      <remap from="sonar5" to="/sonar_5"/>
      <remap from="sonar6" to="/sonar_6"/> -->
    </node>
  </group>
</launch>